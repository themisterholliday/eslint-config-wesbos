// Warning when not returning value for function
// This means TS lint is running only on TS files
function getGreeting() {
  return 'howdy';
}

getGreeting();

console.log('object');

const notused = 'test';
